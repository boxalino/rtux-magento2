/**
 * Purchase event track event for Boxalino RealTimeUserExperience
 * (done server-side)
 */
define([], function () {
    'use strict';
    return {

        supports() {
            return false;
        },

        execute() {}
    };
});
